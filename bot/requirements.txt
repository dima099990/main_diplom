# Telegram bot
python-telegram-bot==21.*

# AI (DeepSeek через OpenAI API)
openai

# Django ORM (та же версия что в основном проекте)
django

# Настройки из .env
python-decouple

# База данных
# SQLite встроен в Python — ничего не нужно
# Если проект на PostgreSQL — добавь: psycopg2-binary

# Django-зависимости (нужны для django.setup())
whitenoise
django-cors-headers
